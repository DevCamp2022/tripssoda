<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.devcamp.tripssoda.mapper.InquiryHistoryMapper">
    <insert id="insertInquiryHistory" parameterType="InquiryHistoryDto">
        INSERT INTO inquiry_history
        (id, inquiry_id, item, before_content, updated_by)
        VALUES (#{id}, #{inquiryId}, #{item}, #{beforeContent}, #{updatedBy})
    </insert>
</mapper>